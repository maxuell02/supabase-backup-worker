# Chave de autenticação entre o backend da Lovable e este worker.
# Gere um valor aleatório forte, ex: openssl rand -hex 32
WORKER_AUTH_KEY=troque-por-uma-chave-forte-aleatoria

# Tamanho máximo de upload aceito pelo worker (usado no restore com upload de arquivo)
MAX_UPLOAD_SIZE_MB=500
